/**
 * Barrel re-export for backward compatibility.
 * Import directly from 'types/auth', 'types/user', or 'types/chat'
 * when you only need one domain's types (ISP).
 */
export * from './auth';
export * from './user';
export * from './chat';

// ---- legacy content removed – kept as re-exports only ----
