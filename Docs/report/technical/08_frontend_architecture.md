# Frontend Architecture

The client is a React 19 TypeScript SPA built with Vite. `router.tsx` defines public, protected, and ADMIN-protected routes. Feature code is organised under components, page components, hooks, context, services, and types. Axios integration and navigation/error handling live under `Client/src/http` and `Client/src/services`.

No frontend automated test files were found during this inspection.

