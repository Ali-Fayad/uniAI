# Performance and Scalability

The retrieval pipeline bounds programme and tuition result sizes, uses parameterized SQL, grouped aggregates, and deterministic ranking/compression. Context budgets bound history/retrieval text before provider invocation. Graduate schema migrations add foreign-key and lookup indexes.

No repository-backed load benchmark or capacity figure was found. Runtime scalability claims should therefore remain qualitative.

