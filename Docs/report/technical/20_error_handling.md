# Error Handling

The shared exception layer maps domain/application failures to HTTP responses. Chat protects ambiguous/unsupported graduate queries with safe clarification before retrieval, detects empty/fallback provider results, and records fallback/budget outcomes. Controller input uses Jakarta validation in many commands; error response shapes are not globally uniform.

