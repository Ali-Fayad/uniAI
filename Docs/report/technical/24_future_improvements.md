# Future Improvements

Future work should be stated as proposals: complete the Google OAuth callback, harden CORS and deployment configuration, add frontend tests and end-to-end tests, enforce academic detail SQL bounds, validate unsupported comparison combinations before retrieval, operationalize screenshots with safe seeded accounts, and add performance/load measurements. These are not claimed as implemented.

