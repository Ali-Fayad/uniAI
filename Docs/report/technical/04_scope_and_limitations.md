# Scope and Limitations

In scope: the functionality in `Client/src`, `Server/src`, root Docker configuration, and Flyway migrations. Out of scope for factual claims: production uptime, real user adoption, provider availability, and unexecuted Docker integration behavior. Google sign-in is partial: URL creation is implemented, while a code-exchange callback endpoint was not confirmed.

