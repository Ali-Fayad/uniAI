# Problem and Motivation

The codebase addresses fragmented university and graduate-program information by combining a conventional web application with retrieval-backed chat. The repository supports verified university, programme, tuition, language, and admission data stored with source URLs. This is an implementation inference from the graduate schema and retrieval adapters, not a measured user-study claim.

